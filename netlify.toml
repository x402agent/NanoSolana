# NanoClawd UI deployment
[build]
  base = "ui"
  command = "npm install && npm run build"
  publish = "../dist/control-ui"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
