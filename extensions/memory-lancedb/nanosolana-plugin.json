{
  "id": "memory-lancedb",
  "name": "Memory LanceDB",
  "description": "Solana Clawd Go vector memory with LanceDB — semantic search for ScgVault entries",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "dbPath": { "type": "string", "default": "~/.nanosolana/lancedb", "description": "LanceDB storage path" },
      "embeddingModel": { "type": "string", "default": "openrouter/healer-alpha" }
    }
  }
}
